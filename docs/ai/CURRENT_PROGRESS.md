> Index only. Check git/PRs for live state.

Mature Stencil component library; six public components + React wrappers.
Docs: `readme.md` (integration overview) · per-component `src/components/*/readme.md`.
Tests: e2e per component; unit coverage limited — see `VERIFICATION.md`.
In-flight: check git log, branch, `gh pr list`.

When closing work: add one "Recently landed" line with PR link below.

### Recently landed
- _none tracked_
